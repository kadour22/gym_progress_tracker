version: '3.9'

services:
  app:
    build: .
    container_name: django_app
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - .:/app
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8080"
 